import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  spinnerContainer: {
    position: 'absolute',
    zIndex: 2,
    width: '100%',
    height: '100%',
  },
});

export default styles;
