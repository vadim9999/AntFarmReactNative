import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  messageBoxSuccess: {
    borderColor: 'green',
    borderWidth: 1,
    borderRadius: 10,
    padding: 15,
  },
  messageBoxError: {
    borderColor: 'red',
    borderWidth: 1,
    borderRadius: 10,
    padding: 15,
  },
});

export default styles;
